#include <stdio.h>
/**
 * main - prints name of program
 * @argc: argument count
 * @argv: argument vector 
 * return 0;
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
